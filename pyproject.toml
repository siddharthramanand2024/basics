[project]
name = "basics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.14.5"

dependencies = [
    "autopep8==2.3.2",
    "faker>=37.0.0",
    "jupyter==1.1.1",
    "numpy>=2.0.0",
    "pandas==3.0.3",
    "plotly>=6.0.0",
    "requests==2.34.2",
    "streamlit>=1.45.0",
]

[project.scripts]
weatherapi = "app.main:main"

[tool.uv]
package = true

[tool.setuptools.packages.find]
include = ["app*"]
